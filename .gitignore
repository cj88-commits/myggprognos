# Dependencies
node_modules/
.wrangler/

# Build output
frontend/dist/
worker/dist/

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# Forecast pipeline caches / generated static features (regenerable; see
# scripts/prepare_grid.py, scripts/prepare_static_features.py). The full
# ~18k-cell grid.json + cell_features.json are derived from
# sweden_boundary.geojson (committed) + config.py and cached by
# forecast.yml via actions/cache, not git -- so they stay out of the repo.
data/cache/
data/static/*.tif
data/static/*.gpkg
data/static/grid.json
data/static/cell_features.json

# NOTE: data/generated/ is intentionally NOT ignored. forecast.yml commits
# data/generated/latest on every run (see its "Commit and push updated
# forecast" step) and deploy-pages.yml reads it straight from the repo to
# bundle with the site; ignoring it here silently broke that commit step
# (git add failed on an explicitly-ignored path) and left production
# permanently serving the small sample dataset from frontend/public/data/latest.

# Claude Code local session settings (not part of the project)
.claude/

# Environment / secrets
.env
.env.local
*.local

# Editor / OS
.DS_Store
.vscode/
.idea/
*.log

# Coverage
coverage/
