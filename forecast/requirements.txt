pandas>=2.2
numpy>=1.26
shapely>=2.0
httpx>=0.27
PyYAML>=6.0
pytest>=8.0

# Optional, only required for real (non-sample) static-feature generation
# from ESA WorldCover + Copernicus DEM tiles (scripts/download_static_gis_data.py,
# scripts/prepare_static_features.py --real). Not needed for sample mode or
# for the scheduled pipeline, which reads the already-committed
# data/static/cell_features.json rather than recomputing it -- see forecast.yml.
# geopandas>=0.14
# rasterio>=1.3

# Optional, only required for the NMD2023 override path (see
# static_features.py's module docstring and scripts/download_nmd_data.py) --
# reprojects cell coordinates from WGS84 into NMD's native SWEREF99 TM.
# pyproj>=3.6
